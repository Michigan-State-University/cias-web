import styled from 'styled-components';
import { flex, margin } from '../BaseComponentStyles';

const Row = styled.div`
  display: flex;
  flex-direction: row;
  ${flex};
  ${margin};
`;

export default Row;
