import styled from 'styled-components';

export const StyledCsvFileExport = styled.div`
  display: flex;

  .button {
    border: 0;
    padding: 0;
    background-color: transparent;
    outline: none;
  }

  .button:hover {
    cursor: pointer;
  }
`;
