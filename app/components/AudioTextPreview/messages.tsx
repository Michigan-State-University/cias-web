import { defineMessages } from 'react-intl';

export const scope = 'app.components.AudioTextPreview';

export default defineMessages({
  readText: {
    id: `${scope}.readText`,
    defaultMessage: 'Read text',
  },
});
