import { defineMessages } from 'react-intl';

export const scope = 'app.components.EventCollapse';

export default defineMessages({
  eventNameLabel: {
    id: `${scope}.eventNameLabel`,
    defaultMessage: 'Name of your event',
  },
});
