import { defineMessages } from 'react-intl';

export const scope = 'app.components.FilterTile';

export default defineMessages({
  clearFiltersText: {
    id: `${scope}.clearFiltersText`,
    defaultMessage: `Reset filters to default values`,
  },
});
