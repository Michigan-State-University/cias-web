import { defineMessages } from 'react-intl';

export const scope = 'app.components.CollaboratingIndicator';

export default defineMessages({
  title: {
    id: `${scope}.title`,
    defaultMessage: 'Intervention with collaborators indicator',
  },
});
