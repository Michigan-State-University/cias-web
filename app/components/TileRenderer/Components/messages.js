import { defineMessages } from 'react-intl';

export const scope = 'app.components.TileRerender.Components';

export default defineMessages({
  defaultLabel: {
    id: `${scope}.defaultLabel`,
    defaultMessage: 'Create new',
  },
});
