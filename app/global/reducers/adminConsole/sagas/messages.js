import { defineMessages } from 'react-intl';

export const scope = 'app.globals.reducers.adminConsole';

export default defineMessages({
  resetAudioSuccess: {
    id: `${scope}.resetAudioSuccess`,
    defaultMessage: 'The request to recreate all audio is being processed.',
  },
});
