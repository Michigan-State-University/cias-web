const borders = {
  borderRadius: '10px',
  borderStyle: 'solid',
  borderWidth: '1px',
};

const paddings = {
  small: '12px',
  regular: '16px',
};

const boxShadows = {
  1: '0px 0px 50px rgba(0, 0, 0, 0.08)',
};

const elements = {
  navbarHeight: 70,
};

export { borders, paddings, boxShadows, elements };
