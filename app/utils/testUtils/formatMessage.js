/**
 * Function that returns `defaultMessage` from message object
 * @param {string}  defaultMessage
 * @returns {string}
 */
export const formatMessage = ({ defaultMessage }) => defaultMessage;

/**
 * Function that returns `id` from message object
 * @param {string} id
 * @returns {string}
 */
export const formatMessageId = ({ id }) => id;
