/*
 * InterventionCreateButton Messages
 *
 * This contains all the text for the InterventionCreateButton component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.InterventionCreateButton';

export default defineMessages({
  create: {
    id: `${scope}.create`,
    defaultMessage: 'Create a new Session',
  },
});
