/*
 * HealthSystemsPanel Messages
 *
 * This contains all the text for the HealthSystemsPanel container.
 */
import { defineMessages } from 'react-intl';

export const scope = 'app.containers.HealthSystemsPanel';

export default defineMessages({
  healthSystems: {
    id: `${scope}.healthSystems`,
    defaultMessage: 'Health systems',
  },
});
