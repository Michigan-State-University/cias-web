/*
 * EmptySessionPage Messages
 *
 * This contains all the text for the EmptySessionPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Interventions.components.EmptySessionPage';

export default defineMessages({
  addFirstScreen: {
    id: `${scope}.addScreen`,
    defaultMessage: 'Add first screen',
  },
});
