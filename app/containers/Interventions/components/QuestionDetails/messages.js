import { defineMessages } from 'react-intl';

export const scope = 'app.containers.QuestionDetails';

export default defineMessages({
  placeholder: {
    id: `${scope}.placeholder`,
    defaultMessage: 'Type answer here...',
  },
});
