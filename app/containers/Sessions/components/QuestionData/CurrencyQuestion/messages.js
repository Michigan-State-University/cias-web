import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Currency';

export default defineMessages({
  amountPlaceholder: {
    id: `${scope}.amountPlaceholder`,
    defaultMessage: 'E.g. 121.12',
  },
});
