import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Phone';

export default defineMessages({
  placeholder: {
    id: `${scope}.placeholder`,
    defaultMessage: 'Respondent answer goes here...',
  },
});
