import { defineMessages } from 'react-intl';

export const scope = 'app.containers.NotAvailablePage';

export default defineMessages({
  pageTitle: {
    id: `${scope}.pageTitle`,
    defaultMessage: 'Study not available',
  },
});
