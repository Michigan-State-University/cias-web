/*
 * ChatWidget Messages
 *
 * This contains all the text for the ChatWidget container.
 */
import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ChatWidget';

export default defineMessages({
  iconAlt: {
    id: `${scope}.iconAlt`,
    defaultMessage: 'Open chat',
  },
});
