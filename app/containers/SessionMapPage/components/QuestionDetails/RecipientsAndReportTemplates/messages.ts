import { defineMessages } from 'react-intl';

export const scope = 'app.containers.RecipientsAndReportTemplates';

export default defineMessages({
  recipientsAndReportTemplates: {
    id: `${scope}.recipientsAndReportTemplates`,
    defaultMessage: 'Recipients & report templates',
  },
});
