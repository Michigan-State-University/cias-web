class ScreenType {
  /**
   * @param  {string} id
   * @param  {string} name
   */
  constructor(id, type, name) {
    this.id = id;
    this.type = type;
    this.name = name;
  }
}

export default ScreenType;
