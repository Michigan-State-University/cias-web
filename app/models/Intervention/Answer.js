class Answer {
  /**
   * @param  {string} text
   * @param  {boolean} isSelected=false
   *
   */
  constructor(text, isSelected = false) {
    this.text = text;
    this.isSelected = isSelected;
  }
}

export default Answer;
