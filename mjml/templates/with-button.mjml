<mjml>
  <mj-head>
    <app-header />
  </mj-head>
  <mj-body>
    <app-layout
      header="{HEADER_TEXT}"
      mainText="{MAIN_TEXT}"
      buttonText="{BUTTON_TEXT}"
      buttonUrl="{BUTTON_URL}"
      footerText="{FOOTER_TEXT}"
    />
  </mj-body>
</mjml>
